h1 ツイート作成

- if @tweet.errors.present?
  ul#error_explanation
    - @tweet.errors.full_messages.each do |message|
      li= message

= form_with(model: @tweet, local: true, url: confirm_tweets_path) do |tweet|
  .form-group
    = tweet.label :context
    = tweet.text_area :context, rows: 5, class: 'form-control'
  = tweet.submit '作成する', class: 'btn btn-primary'
